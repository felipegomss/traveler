/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["source.unsplash.com", "flowbite.s3.amazonaws.com"],
  },
  reactStrictMode: false,
};

export default nextConfig;
